# Backend .env example
# Copy this file to .env and fill values as needed.

# Port for server
PORT=3000

# MongoDB connection URI
# MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/dbname

# JWT secret used to sign tokens for users
USER_JWT_SECRET=Tajagn@123

# Shareable link host (frontend host)
SHARABLE_LINK_HOST=https://secondbrain001.netlify.app

# Allowed web origins for CORS (comma separated)
ALLOWED_WEB_ORIGINS=https://secondbrain001.netlify.app

# If you don't want to connect to a DB while testing locally, set:
# SKIP_DB=true

# Google Gemini API key (used by youtube summarization)
# GEMINI_API_KEY=your_gemini_api_key_here

# (OpenAI key removed) If you previously used OpenAI, it's no longer required.
